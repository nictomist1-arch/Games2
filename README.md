
# 🛡️ Проект: Arena Survivors — Учебная разработка игры на JavaScript

> **Внутренняя документация проекта**  
> *Для учеников и наставников. Цель: создать модульную, расширяемую игру в браузере, используя принципы ООП и командную разработку.*

---

## 🎮 Концепция и видение

Мы создаем игру в жанре **выживание на арене** с элементами RPG и крафта. Игрок выбирает героя, сражается на локациях, получает награды и улучшает персонажей.

**Ключевые особенности:**
- 🧩 **Модульность** — каждый модуль можно заменить или отключить.
- 📱 **Адаптивность** — игра корректно отображается на любом экране (никакого скролла страницы, только внутренние скроллы).
- 💾 **Локальные сохранения** — прогресс хранится в `localStorage`.
- ⚙️ **Итерационная разработка** — каждый новый кусочек кода работает сразу.

---

## 📁 Структура проекта

Проект организован по принципу *разделения ответственности* (Separation of Concerns). У нас есть чёткое разделение на:

1. **Ядро (Core)** — игровая логика, сущности, математика.
2. **Пользовательский интерфейс (UI)** — отрисовка, обработка кликов.
3. **Состояние (State)** — единый источник правды (Single Source of Truth).

```
arena-survivors/
│
├── index.html          # Точка входа (разметка, подключение скриптов)
├── style.css           # Стилизация (адаптив, темы, сетки)
├── README.md           # Этот документ
│
├── js/
│   ├── game.js         # Главный файл инициализации (связывает всё воедино)
│   ├── core/           # Модули ядра
│   │   ├── GameState.js    # Хранилище данных (ресурсы, герои, инвентарь)
│   │   ├── Hero.js         # Класс героя (характеристики, уровень, инвентарь)
│   │   ├── Item.js         # Предметы (оружие, броня, расходники)
│   │   ├── Arena.js        # Логика арены (бой, волны, награды)
│   │   └── CraftSystem.js  # Система крафта (рецепты, материалы)
│   │
│   └── ui/             # Модули интерфейса
│       ├── UIManager.js     # Управление экранами, обновление UI
│       ├── HeroScreen.js    # Отрисовка списка героев
│       ├── ShopScreen.js    # Магазин
│       └── ModalManager.js  # Модальные окна (инвентарь, экипировка)
│
└── assets/             # (Позже) изображения, звуки, шрифты
    ├── sounds/
    └── sprites/
```

**Правило:**  
- Модули из `core/` **не знают** о существовании UI.  
- Модули из `ui/` только читают состояние и подписываются на изменения.  
- `game.js` — дирижёр, который всё собирает.

---

## ⚡ Ядро (Core) — объяснение сущностей

### 🦁 Hero.js
Главный класс игры. Отвечает за:
- Хранение базовых характеристик (HP, атака, защита, скорость).
- Уровень и опыт (формулы прироста статов).
- Инвентарь героя (массив из 9 слотов).
- Методы экипировки/снятия предметов.

```javascript
class Hero {
    constructor(id, name, baseStats) {
        this.id = id;
        this.name = name;
        this.level = 1;
        this.exp = 0;
        this.stats = { ...baseStats }; // Текущие статы (с учётом экипировки)
        this.baseStats = { ...baseStats }; // Базовые (без учёта шмота)
        this.inventory = new Array(9).fill(null); // 9 слотов
        this.availableSkills = []; // Доступные навыки для выбора
        this.selectedSkills = []; // Выбранные навыки
    }

    addExp(amount) { /* Проверка на повышение уровня */ }
    levelUp() { /* Увеличение статов, получение очка навыка */ }
    equipItem(item, slotIndex) { /* Положить предмет в слот */ }
    unequipItem(slotIndex) { /* Снять предмет */ }
    recalcStats() { /* Пересчитать статы с учётом экипировки */ }
}
```

### 🗡️ Item.js
Базовый класс для всего, что можно положить в инвентарь.

```javascript
class Item {
    constructor(id, name, type, rarity, statsBonus = {}) {
        this.id = id;
        this.name = name;
        this.type = type; // 'weapon', 'armor', 'consumable'
        this.rarity = rarity; // 'common', 'rare', 'epic'
        this.statsBonus = statsBonus; // { hp: +10, attack: +5 }
        this.skillModifier = null; // Если предмет модифицирует навык
    }
}
```

**Типы предметов:**
- `weapon_melee` — ближний бой.
- `weapon_ranged` — дальний бой (требует перезарядки).
- `armor` — броня (шлем, нагрудник и т.д.).
- `consumable` — расходник (зелье, бомба).

### ⚔️ Arena.js
Менеджер боя. Содержит логику:
- Запуск арены (проверка ресурсов).
- Генерация волн врагов.
- Цикл боя (real-time).
- Завершение матча и расчёт наград.

```javascript
class Arena {
    constructor(locationConfig) {
        this.location = locationConfig.id;
        this.costType = locationConfig.costType;
        this.duration = 60; // секунд
        this.enemies = [];
        this.rewards = {};
    }

    start(hero) { /* Инициализация боя */ }
    update(deltaTime) { /* Игровой цикл */ }
    end(victory) { /* Выдача наград */ }
}
```

### 🔨 CraftSystem.js
Управление крафтом:
- Проверка наличия ресурсов.
- Создание предмета.
- Открытие новых рецептов.

```javascript
class CraftSystem {
    constructor() {
        this.availableRecipes = [];
        this.allRecipes = []; // База данных рецептов
    }

    unlockRecipe(recipeId) { /* Добавить в доступные */ }
    craft(recipeId, hero) { /* Потратить ресурсы, создать предмет, положить герою */ }
}
```

### 💾 GameState.js
Центральное хранилище (Singleton). Все модули подписываются на его изменения.

**Структура состояния:**

```javascript
const GameState = {
    resources: { proviziya: 10, toplivo: 5, instrumenty: 3 },
    materials: { wood: 0, metal: 0, cloth: 0 },
    heroes: [], // Массив экземпляров Hero
    currentHeroId: null,
    unlockedLocations: ['forest'], // Открытые локации
    unlockedRecipes: [],
    settings: { sound: true, music: true },
    
    // Методы
    subscribe(listener) { /* Добавить слушателя */ },
    notify() { /* Уведомить всех об изменениях */ },
    save() { /* Сохранить в localStorage */ },
    load() { /* Загрузить из localStorage */ }
}
```

---

## 🎨 Визуализация и UI (HTML/CSS)

### Философия вёрстки
Главное правило: **никакого скролла на всей странице**.  
Скролл может быть только внутри отдельных блоков (список героев, магазин), если контент не помещается.

### Структура HTML (из вашего шаблона)

`index.html` делится на 3 вертикальные зоны:

1. **Header (шапка)** — всегда видна, содержит ресурсы.
2. **Main (рабочая область)** — занимает всё оставшееся место, внутри переключаются экраны.
3. **Nav (нижнее меню)** — всегда видно, навигация по разделам.

```html
<div class="game-container">
    <header class="game-header">...</header>
    <main class="game-screen" id="gameScreen">...</main>
    <nav class="game-nav">...</nav>
</div>
```

### CSS-классы и их назначение

| Класс              | Назначение |
|--------------------|------------|
| `.game-container`  | Флекс-контейнер на всю высоту, направление колонка |
| `.game-header`     | Фиксированная высота, `flex-shrink: 0` |
| `.game-screen`     | `flex: 1`, внутри `position: relative` |
| `.screen`          | Все экраны скрыты (`display: none`), активный — `display: block` |
| `.screen.active`   | Показывает текущий экран |
| `.modal`           | Полноэкранный затемнённый фон, поверх всего |
| `.modal-content`   | Центрированное окно с внутренним скроллом |

### Адаптивность
Медиазапрос для мобильных устройств:
```css
@media (max-width: 600px) {
    .game-header { flex-direction: column; }
    .resources { justify-content: center; }
    .nav-btn { font-size: 1rem; }
}
```

**Важно:** Все элементы используют относительные единицы (`rem`, `%`, `vh`), чтобы масштабироваться.

---

## 🔄 Взаимодействие модулей

Рассмотрим, как открывается экран героев:

1. Пользователь кликает на кнопку "Герои" в нижнем меню.
2. `UIManager` обрабатывает клик и вызывает `showScreen('heroes')`.
3. `HeroScreen.js` (который подписан на изменения GameState) получает актуальный список героев и отрисовывает их.
4. Если в будущем мы добавим нового героя (например, через разблокировку), `GameState` уведомит всех подписчиков, и `HeroScreen` перерисуется автоматически.

**Паттерн Наблюдатель (Observer):**  
`GameState` хранит массив `_listeners`. Любой модуль может подписаться через `subscribe()` и будет вызываться при каждом изменении состояния.

---

## 👥 Правила командной разработки

### Роли в команде (на выбор)
- **Core-разработчик** — занимается классами `Hero`, `Item`, математикой.
- **UI-разработчик** — верстает новые экраны, стилизует компоненты.
- **State-менеджер** — отвечает за GameState, сохранения, подписки.
- **Тестировщик/документатор** — пишет тесты, проверяет крайние случаи.

### Процесс работы
1. **Планирование** — выбираем задачу из раздела "План итераций".
2. **Ветка в Git** — создаём ветку `feature/название-фичи`.
3. **Разработка** — пишем код, комментируем сложные места.
4. **Code Review** — минимум 1 другой ученик смотрит код.
5. **Мёрж** — после апрува вливаем в `develop`.

### Code Style
- **Имена классов** — с большой буквы, существительные (`Hero`, `Arena`).
- **Имена методов** — глаголы, с маленькой буквы (`attack()`, `move()`).
- **Комментарии** — JSDoc для публичных методов.
- **Форматирование** — 2 пробела для отступа.

---

## 📅 План итераций (Этапы разработки)

### Этап 1: Скелет и навигация (текущий)
- [x] HTML-каркас с экранами.
- [x] Базовая стилизация (цвета, сетки).
- [x] Переключение между экранами.
- [x] Глобальный объект GameState с ресурсами.

### Этап 2: Герои и инвентарь
- [ ] Реализовать класс `Hero` (уровень, статы).
- [ ] Экран героев: отображение списка, выбор активного героя.
- [ ] Модальное окно инвентаря (перетаскивание или клик).
- [ ] Экипировка предметов (хардкодные тестовые предметы).

### Этап 3: Экономика и магазин
- [ ] Система ресурсов (провизия, топливо, инструменты).
- [ ] Пассивная генерация ресурсов (раз в час — имитация).
- [ ] Экран магазина: покупка базовых предметов.
- [ ] Интеграция с GameState (трата ресурсов, получение предметов).

### Этап 4: Система крафта
- [ ] Класс `CraftSystem`.
- [ ] Экран крафта с рецептами.
- [ ] Проверка материалов, создание предмета.
- [ ] Открытие рецептов (пока хардкод).

### Этап 5: Боевая система (MVP)
- [ ] Класс `Arena` (упрощённый бой: игрок vs 1 враг).
- [ ] Экран битвы (таймер, кнопки атаки).
- [ ] Награды за победу (опыт, ресурсы, материалы).
- [ ] Сохранение состояния после боя.

### Этап 6: Продвинутые механики
- [ ] Навыки героев (выбор 1 из 3 при повышении уровня).
- [ ] Расходники (использование в бою).
- [ ] Разблокировка новых героев за прохождение локаций.
- [ ] Звуки и анимации (простые CSS-переходы).

### Этап 7: Финальный полиров
- [ ] Сохранение/загрузка в `localStorage`.
- [ ] Баланс (настройка цифр).
- [ ] Тестирование на мобильных устройствах.
- [ ] Рефакторинг (принципы SOLID, единая ответственность).

---

## 🏠 Задания для самостоятельной работы

*Эти задачи можно брать параллельно с основным планом. Они не ломают основную логику, но добавляют игре глубины.*

1. **Счётчик времени** — Добавить на главный экран отображение текущего времени (реального), чтобы игра чувствовалась "живой".
2. **Звук клика** — Добавить воспроизведение короткого звука при нажатии на любую кнопку (использовать `Audio`).
3. **Система достижений** — Создать модуль `Achievements.js`, который отслеживает события (убито 10 врагов, накоплено 100 ресурсов) и выдает небольшие награды.
4. **Сортировка в инвентаре** — Добавить кнопки "Сортировать по типу/редкости" в модальном окне героя.
5. **Система заданий (квестов)** — Реализовать ежедневные задания: "Проведи 3 матча в лесу", "Потрать 5 топлива". За выполнение давать дополнительную валюту.
8. **Состояние запасов** — Составить класс для склада. На экране меню отображать количество ресурсов по отношению к максимальному объему склада.
9. **Свои рецепты** — Дать игроку возможность создавать свои (открывать без аналога) рецепты крафта, комбинируя предметы (сложная логика валидации).

---

## ✌️ Заключение

Этот проект — не просто игра. Это **конструктор**, который вы будете собирать сами, понимая, как взаимодействуют модули, как управляется состояние и как интерфейс реагирует на данные.

Главное — **не бойтесь экспериментировать**. Если хотите добавить фичу, которой нет в плане — создайте ветку и попробуйте. Важно, чтобы она не ломала основную логику.

---
